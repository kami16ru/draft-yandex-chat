block aside
  div.nav-drawer
    ul.nav-drawer__menu
      include ../../../configs/navigation
        each item in navItems
          li.nav-drawer__menu__item
            a(href=item.href)
              div
                span(class="nav-drawer__menu__item__icon mdi " + item.icon + " h1")
                span(class="nav-drawer__menu__item__content")= item.text
